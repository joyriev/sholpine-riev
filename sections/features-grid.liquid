<div class="features-grid section-{{ section.id }}-padding">
  <div class="container mx-auto px-4">
    <div class="grid grid-cols-2 md:grid-cols-4 gap-6">
      {% for block in section.blocks %}
        <div class="feature-item flex items-start gap-3" {{ block.shopify_attributes }}>
          <div class="feature-icon text-3xl">{{ block.settings.icon }}</div>
          <div>
            <h3 class="feature-title font-bold text-sm">{{ block.settings.title }}</h3>
            <p class="feature-description text-xs text-gray-600">{{ block.settings.description }}</p>
          </div>
        </div>
      {% endfor %}
    </div>
  </div>
</div>

{% schema %}
  {
    "name": "Features Grid",
    "tag": "section",
    "class": "section",
    "settings": [
      {
        "type": "header",
        "content": "Section Padding"
      }, {
        "type": "range",
        "id": "padding_top",
        "min": 0,
        "max": 100,
        "step": 4,
        "unit": "px",
        "label": "Top Padding",
        "default": 32
      }, {
        "type": "range",
        "id": "padding_bottom",
        "min": 0,
        "max": 100,
        "step": 4,
        "unit": "px",
        "label": "Bottom Padding",
        "default": 32
      }
    ],
    "blocks": [
      {
        "type": "feature",
        "name": "Feature",
        "settings": [
          {
            "type": "text",
            "id": "icon",
            "label": "Emoji Icon",
            "default": "✨"
          }, {
            "type": "text",
            "id": "title",
            "label": "Title",
            "default": "Feature Title"
          }, {
            "type": "text",
            "id": "description",
            "label": "Description",
            "default": "Feature description goes here."
          }
        ]
      }
    ],
    "presets": [
      {
        "name": "Features Grid",
        "blocks": [
          {
            "type": "feature",
            "settings": {
              "icon": "📦",
              "title": "Free Returns",
              "description": "No questions asked."
            }
          }, {
            "type": "feature",
            "settings": {
              "icon": "🍊",
              "title": "6,000,000+ Bottles",
              "description": "over 6M bottles sold worldwide."
            }
          }, {
            "type": "feature",
            "settings": {
              "icon": "👁️",
              "title": "100,000+ Reviews",
              "description": "over 100,000 5+ star reviews."
            }
          }, {
            "type": "feature",
            "settings": {
              "icon": "🌸",
              "title": "Viral fragrance brand",
              "description": "10,000+ views & dazzling social reviews."
            }
          }
        ]
      }
    ]
  }
{% endschema %}
<style>
  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top }}px;
    padding-bottom: {{ section.settings.padding_bottom }}px;
  }
</style>